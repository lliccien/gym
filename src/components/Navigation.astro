---
// Componente de navegación con pestañas
const { currentPage = 'today' } = Astro.props;
---

<div class="border-b border-gray-200 mb-4 sm:mb-6">
	<nav class="-mb-px flex justify-center sm:justify-start space-x-4 sm:space-x-8" aria-label="Tabs">
		<a 
			href="/" 
			class={`py-3 px-1 border-b-2 font-medium text-sm ${
				currentPage === 'today' 
					? 'border-blue-500 text-blue-500 font-semibold' 
					: 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
			}`}
		>
			🏋️‍♂️ Entrenamientos
		</a>
		<a 
			href="/antropometria" 
			class={`py-3 px-1 border-b-2 font-medium text-sm ${
				currentPage === 'antropometria' 
					? 'border-blue-500 text-blue-500 font-semibold' 
					: 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
			}`}
		>
			📊 Antropometría
		</a>
	</nav>
</div>
